# Kirby Task Board - Environment Variables

# API Key for authentication (required for POST, PUT, DELETE operations)
# Elvis and Kirby will use this key to modify tasks
API_KEY=kirby-taskboard-2025-elvis

# Port for the server (defaults to 3000)
PORT=3000

# SQLite database file path (defaults to ./tasks.db)
DATABASE_PATH=./tasks.db
