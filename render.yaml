services:
  - type: web
    name: kirby-taskboard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: API_KEY
        value: kirby-taskboard-2025-elvis
      - key: NODE_ENV
        value: production
      # IMPORTANT: Add DATABASE_URL in Render Dashboard Environment Variables
      # Value: postgresql://postgres:xrRKWsawso1@db.ckzezftoemljlepyywoj.supabase.co:5432/postgres
